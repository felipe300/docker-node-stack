version: '3'

services:
  backend:
    build: .
    ports:
      - '8000:8000'
      - '9229:9229' # for debugging
    volumes:
      - .:/usr/src/app
      - /usrsrc/app/node_modules
    command: npm run start:docker
    environment:
      PORT: 8000
